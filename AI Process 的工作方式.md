# AI Process 的工作方式：把不可逆的大承诺，换成可逆的小承诺
> 这份文档解释这套流程为什么是现在这个样子。它不是功能清单——每一个设计都有要解决的问题，多数设计是被前一个设计留下的旧问题逼出来的。建议按顺序读：每一节的结尾，是下一节的开头。
## 一、问题的起点
复杂需求的失败，往往不在执行，而在开工之前：**信息最不全的时刻，做出了最不可逆的承诺**。此刻对需求、对旧代码、对问题本身的理解都还是残缺的，残缺的理解直接落成代码，错误的暴露时机就被推到了最贵的位置——写完才发现，发现即返工。
这套流程针对的正是这一点：**把一次大承诺，换成多次小承诺。错误要暴露得早，纠正要便宜。**
## 二、讨论期：分歧消化在动代码之前
流程的第一段不是写代码，是讨论。需求方写 `需求.txt`，AI 回应 `v1.md`，需求方逐条回复，AI 再出 v2——版本链一路延长，直到双方对需求边界的认识重合。
每一轮的产出是一版更完整的**方案**。为方案的质量把关的机制叫**缺口模型**：每轮明确当前方案还缺什么、哪里站不住，包括 AI 没有意识到自己不知道的部分。缺口的补全方式由提问规则决定：能从项目文件确认的，AI 先搜索；查不到的，才问需求方。需求方有限的注意力，只花在文件里查不到的问题上。
复杂度藏在哪，讨论就长到哪。分歧在动代码之前被逐轮消化——这是第一重“切小”：把一次性的理解风险，切成多轮可纠正的偏差。
但讨论只解决了“想清楚”。理解再透，收敛出来的仍是一整块方案，动手那一刻依然是一次性的。**怎么把方案本身也切小？**
## 三、固化与拆分：流程深度跟着复杂度走
讨论收敛后，复杂方案先固化为一份**实施文档**：复杂度分析、需求边界、技术方案，全部落成文字。它是讨论与执行之间的翻译层，也是后续拆分与执行的唯一依据。
方案足够大时，再拆分为若干步骤目录（01-xxx、02-xxx……），每个步骤一份自己的方案文档，按编号形成执行顺序。拆分由 AI 完成，人只做确认。
拆分真正的支撑是**递归**：某个步骤如果长出了自己的讨论链，它就升级为独立子主题，整套流程在内部原样再走一遍。复杂度是嵌套的——崩溃问题里嵌着线程模型，线程模型里嵌着兜底策略。流程同构递归，多深的复杂度都装得下，不需要为更深层发明新机制。
需要强调：**拆分不是必选项，实施文档也不是。** 问题足够简单时，讨论收敛后直接执行，一次“改”指令就进入代码。流程的深度跟着复杂度走——简单需求不背着复杂流程，复杂需求不缺任何一层。这本身就是“切小”原则的另一面：不为小问题支付大流程的成本。
到这里，事前的路径已经完整。但任何事前的拆分，都预见不了执行期才暴露的现实——方案落进代码之后，总会出现讨论时无人想到的问题。**意外来了怎么办？**
## 四、修正期：意外独立立案
执行期暴露的问题，不打回原讨论重来，而是在对应步骤下开一个**结果微调**主题：它有自己的需求文件、自己的版本链、自己的结论，账目汇总回父主题。
独立立案的意义在于**分账**。意外混进原任务，“当初方案对不对”和“执行发现了什么新情况”很快搅成一笔糊涂账；分开之后，每个意外都是一条可追溯的记录：何时暴露、为何事先没想到、如何处理。**意外不被隐藏，也不被放大——它只是被如实记账。**
这个机制同时是流程的诚实声明：前面的所有设计都不能保证不出意外。承认这一点，是与“声称一次设计到位”的方式最大的区别。
## 五、两个贯穿条件：时间与分工
上面四个阶段描述的是流程的时间线。有两个东西不属于任何阶段，却贯穿所有阶段——它们在时间上和分工上，让这条时间线得以成立。
**留痕与复关系。** 每个阶段的产出都是文件：版本链、回复、实施文档、微调记录。复关系（上下文重建）按通用模板扫描当前主题目录，在任意时点、任意层级恢复完整的思考现场——隔夜、换人、换 AI 之后，现场仍在。流程的存续不依赖任何一方的记性：上下文不在谁的大脑里，在目录里。
**人闸与制备。** 全流程的分工只有一条线：AI 负责制备——搜文件、写文档、拆目录、重建现场，一切可重复、可交代的劳动；人负责判断——每轮回复、收敛确认、执行触发，一切需要权衡和担责的决定。这条分工直接决定错误成本：方向性错误活不过一轮，下一轮回复即纠正，成本以分钟计；制备性错误重做即可，损失近乎为零。复杂问题里最稀缺的是人的判断力，流程的全部自动化，都是为了把这份资源集中到判断上。
## 六、边界
1. **流程要求需求写得清。** 要求与验收标准能写清楚的任务，分歧可以在动手前消化；审美类“看到才知道对不对”的任务，流程只能记录商量结果，不保证收敛。
2. **人闸是唯一串行环节。** 所有自动化最终汇到人的判断上。复杂度再上一个量级，先到顶的不是 AI 的处理能力，是人的审阅带宽。
3. **流程管理复杂度，不消灭复杂度。** 递归讨论、分层拆分、微调记账，做的都是把大问题排队变成小问题；排队之后，每个小问题仍需被认真对待。
## 结语
复杂问题的困难，不在于它大，在于它逼人在信息不全时做出不可逆的承诺。这套流程把那一次承诺换成了一串可逆的小步：**分歧逐轮消化，共识固化成文，大方案按需拆分并可递归，意外独立立案如实记账**——流程深度始终与复杂度相称，每一步的错误都暴露得早、纠正得便宜。
一个复杂需求走完全程回头看：它没有被谁“解决”过，它只是被拆成了一串小问题，而这串小问题，都已经有了答案。
