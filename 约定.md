# AIProcess 约定

> 阅读顺序：先看 README（怎么用），再看本文（规则是什么）。

## 一、目录模型

- **主题目录**：一个独立对话域，包含完整版本链（见第二章）。
- **结果微调/NN**：主题执行后的二次沟通轮次目录，NN 为两位数字（工具按 `^\d{2}$` 识别）。
- **实施步骤/01-xxx**：执行阶段的步骤拆分目录，`01-xxx` 下放置同名 md 文档。
- 层级可递归：步骤目录内可再按同样规则嵌套（结果微调、实施步骤），每层都是独立对话域。
- `.aiprocess/`：工具内部数据（如总结），遍历版本链时跳过。

## 二、版本链命名

| 文件 | 含义 | 规则 |
|---|---|---|
| `需求.txt` | 人写的需求输入 | 每轮主题的起点 |
| `vN.md` | AI 的回应（分析/提问） | `^v(\d+)\.md$`，N 递增 |
| `对vN的回复.txt` | 人对 vN 的作答 | 与 vN 一一对应 |
| `实施文档.md` | 定稿（复杂度分析/拆分判断/边界/方案） | 每主题一份 |
| `已实施.md` | 改吧执行的结果报告（有实施文档时） | 见第三章 |
| `v(N+1).md` | 改吧执行的结果（无实施文档时） | 沿用版本链 |

## 三、三阶段

1. **沟通对齐**：复需求 → AI 出 `v1.md`（分析 + 待确认问题区）→ 建回复（提取问题生成 `对v1的回复.txt` 骨架）→ 复回复 → `v2.md`…… 循环直到 AI 无新问题。
2. **实施文档**：勾"实"复回复 → AI 出 `实施文档.md`（开头复杂度分析 + 是否拆分判断 + 需求边界 + 技术方案）。
3. **执行**：复执行两策略——
   - **改吧**：直接执行；结果写 `已实施.md`（有实施文档）或 `v(N+1).md`（无实施文档），AI 按 `execute/result_doc.md` 模板撰写。
   - **步目**：必拆，建 `实施步骤/01-xxx/01-xxx.md` 步骤目录，建完即停（只拆不执行）。

## 四、拆分规则与格式

- **规则（可判断、有选择）**：实施文档里已有的拆分方案优先于执行指令中的通用规则。仅当实施文档未约定拆分方案时，才用指令中的通用规则拆分。
- **格式（固定、与规则无关）**：`实施步骤` 目录 + `01-xxx` 子目录 + 同名 `01-xxx.md`；结果微调轮次用两位数字编号。
- **粒度要求**：适中——简单功能别拆成几十份，复杂功能也别只拆两三份。

## 五、代码结构

- 分层：`app/modules/` 下 core（启动/配置/日志）→ session（窗口会话）→ ui（界面）→ business（业务），保持单向依赖（工具模块不依赖业务模块，被调用方不知道调用方）。
- 提示词模板集中在 `app/templates/`（可复用元素模板在 `templates/elements/`）；执行策略清单在 `app/modules/business/services/ExecuteStrategyRegistry.ahk`；编辑器列表由 `app/config/filetools.json` 数据驱动。
- business 层内部：六个领域行为（建需求/复需求/建回复/复回复/复执行/复关系）是一等公民——`behaviors/`（AgentActionBase 基类 + AgentActions 门面 + actions/ 每行为一类），公共能力在 `services/`（DomainConventions 约定单源 / VersionChain / PromptElements 提示词元素库 / EditorOpener / ExecuteStrategyRegistry / ActionGuard）。
- 现状附录（迁移中事实，勿当作两套设计）：
  - `Session.ahk` 的窗口会话与 `settings.ini` `[WindowN]` 段双写，以 ini 为准。
  - 主面板 Agent 绑定走 `AgentWindowManager.ahk`（经 Session）；通用 Agent 活动走 `AgentDispatcher.ahk`（直读 ini），总结窗口使用 `SummaryAgent` 段。

## 六、沟通口径

- 对 AI 的提示词：对话式语气、简洁表达，不废话。
- 规则（可判断选择）与格式（固定不变）分开写；行为约束在提示词中说明。
