# 缺口：AI 协作中最容易被漏掉的一类问题

> 本文整理自与 AI 的实际讨论。

## 一、理论起点

这个议题的两块地基，已经在另两份文档里：

- `工具价值.md`：「AI 会悄悄出错——**它会把猜测当成事实写给你，你不追问，它不会主动说**」
- `AI时代的演进方向.md`：「判断求真…**真值缺失 → 采购，不凭空判**」；「校准回路：台账记'当时判断 X → 实际结果 Y'」

整套内容从两个命题长出来。

> **命题一：AI 的每一份输出，都是"在它当前认知状态下能做到的最好猜测"。**

> **命题二：它的输出同时暴露两类缺口——它知道自己缺的，和它不知道自己缺的。**

这两类缺口的区别是根本的：

- **它知道自己缺的**：它会问（待确认问题区），或者它会明写"这里我不确定"。**对它来说这是一个洞。**
- **它不知道自己缺的**：它不会问，也不会标出来——因为它已经把猜测当成了事实。**对它来说那里没有问题，那片地面是平的。**

由此立刻得到一个反直觉的结论：

> **凡是 AI 问出来的，都不是最危险的问题。**
> 最危险的是它没问的那些——那里它在猜，而它不觉得在猜。

这条不是修辞。它就是 `工具价值.md` 那句"你不追问，它不会主动说"的精确化：**"不会主动说"不是态度问题，是结构问题——它没有那个缺口的感觉。**

## 二、五类回复 = 补五种缺口

人在协作里实际给出的回复，可以归成五类。命题一、二让它们不再是并列的清单，而是**同一件事的五个位置**：

| # | 回复类型 | 补的是什么缺口 | 它做什么事 |
|---|---|---|---|
| 1 | 补充背景信息 | **世界的事实**（你手里有，它不知道） | 让猜测停止 |
| 2 | 目标调整 | **要什么 / 不要什么** | 让方向回正 |
| 3 | 不确定性追问 | **未知的事实**（连你也没有） | 把不可见的缺口变成可见 |
| 4 | 已有方案的决策 | **多个解之间的选择** | 从多解收敛到一解 |
| 5 | 实际情况反馈 | **"以为做到"与"实际做到"之差** | 修正偏差 |

按"信息在谁手里"再看一遍，会更清楚：

- **1、2、4**：信息在你手里（事实、目标、取舍），动作是**交出去**；
- **3**：信息**谁手里都没有**，动作只能是**指出去取**——这是五类里唯一"人给不出答案"的一类；
- **5**：信息在执行结果里，动作是**回灌**。

**第 3 类在结构上是特殊的**：前四类补的都是"存在但错位"的信息，只有它补的是"尚不存在"的信息。**所以它的动作和前四类不同——前四类是把信息交出去，它是把信息取回来。** 要取的东西有三样：

| 取什么 | 怎么问 |
|---|---|
| 它的假设是否成立 | "这个条件你确认过在实际代码里成立吗" |
| 方案在什么范围内成立 | "这方案在什么情况下会失效" |
| 有没有更好的解 | "有没有更好的解" / "拿我这个方案和你的做优劣对比" |

三种遵循同一个分工：**它去查，你判断查到的东西真不真。**

## 三、什么时候露头，放过了什么时候付代价

**不是人在不同时间想给不同回复，而是不同类型的缺口，在不同的时间点才具备"暴露条件"。**

| 类型 | 什么时候才会被看见 | 为什么只能在这时 | 放过的代价 |
|---|---|---|---|
| 2 目标调整 | AI 给出方案的那一刻 | 方案是目标的函数；目标错的时候，方案看起来是"对的"，只有看到它，你才知道这不是你要的 | 它做出的是你不想要的东西，哪怕做得很完整——要到验收或用起来时才发现 |
| 1 补充背景信息 | AI 露出它的假设的那一刻 | 它不写出来，你不知道它假设了什么 | 它在错的事实上继续工作，且不知道自己在错——执行后才发现 |
| **3 追问** | **永不主动暴露** | 缺口对它不存在，所以没有任何时刻会抛出信号——**只能靠你** | 这个缺口一直没人管，跟着进了执行——**执行后，它会以第 5 类的形式回来** |
| 4 决策 | AI 枚举完、发问的那一刻 | 选择只能发生在选项成形之后 | 它自己拍板，或停在那里——立刻卡住，或执行后才发现 |
| 5 反馈 | 执行完成的那一刻 | 它的原料是实际结果，结果之前不存在 | 错误的前提被后面接着用——下一轮或再下一轮才发现 |

这张表里还藏着一处区别：**4 和 5 会有人来催你**（它问了、它做完了）；**1、2、3 不会**——它们的"暴露条件"只是那一刻它有可能被看见，没有任何东西会把你拉到那个点上；**而它们的代价又全部显形在"执行后"**。合起来就是：**没人催，也没人当场发现。**

不过五类在这一点上是一样的：**没被处理的缺口不会消失，它会以第 5 类，也就是实际情况反馈的形式回来**——bug、偏差、"还是不对"、"并没有解决"，其中有一部分本质上就是当初没能被问出来的那个缺口。

次序上有一处是固定的：**追问要赶在"决策"之前。** 决策一旦落定，路已经选了，那个数也就不会再有人去取。

## 四、报警：从发现到分类

第三节那张表第 3 行是空白的——**没有任何时刻会主动抛出信号**，只能靠你自己读文档时那一下。

> **报警：读 AI 输出时，某一处让你停下来、读不过去的那一下。**

它有两个特点：**来得早**（在你开始分析之前就响了），**且只给位置、不给内容**（指出"这里不对"，说不出"哪里不对"）。

第二个特点推出这套东西最有用的结论：

> **"说不清"不是需要克服的缺点，它是分类依据。**

所以当场只有一问：**我有没有"不对"的感觉？**

- **没有** → 照常处理，不需要任何判断。
- **有** → 再问一句：**我能不能说清它错在哪？**
  - 能，错在"**事实**" → 【补充背景信息】
  - 能，错在"**要做的事**" → 【目标调整】
  - 说不出，只知道不对 → 【不确定性追问】

第二问测的是**信息在不在你手里**，不是**你懂不懂技术**。

**"我没看懂"也算**——读着别扭、听不明白、不知道它在说什么，都是报警。它常被当成自己不懂技术而放过去，但读不懂本身就是一个待处理的问题。如果一整片都看不懂（需求很清楚，但不知道它会怎么实现），先让它自检一次、把结果列成清单，把"一整片"拆成几条能读的，你在清单上读出报警再追问。

## 五、要练的那一处

报警被写成回复文件里的一个具体问题，它的职责就完成了——**闭环发生在那句话上，不在流程末尾。**

所以这套东西要练的只有一件事：

> **从"说不清"到"一句具体问题"——练这段距离。**

- **不练次数。** 报警多不等于好；不同议题难度不同，次数本身不可比。
- **练缩短。** 感受是模糊的（"这里不对"），写进回复文件的必须具体（"它在假设 X，而这个假设可能不成立"）。

**判据只有一个：你写进回复文件的那句话，能不能被回答。** 能，这条报警的职责就完成了；不能，说明它还没有被缩短完。

模糊的报警不写出来，就只是一个感觉，转瞬即逝；写出来，它就变成一件可以被回答的事。**报警的全部价值，等于你能多快把它写成那句话。**

到这里，两份已有文档和本议题就合上了：

> `AI时代的演进方向.md` 给方向（是判断就自己做）；
> `工具价值.md` 给工具（流程保证假设被问出来）；
> **本理论给中间那一层：判断做不准的那一刻，缺口的形状是什么、它在哪个时间点会暴露、以及该用哪个动作去补。**
> 接口是同一个：**AI 只暴露它知道自己缺的；剩下的靠你的直觉——而直觉的价值，等于你能多快把它写成一句具体问题。**
