# 工具价值

> 这份文档回答一个问题：这个工具为什么值得用、为什么这样设计。
> 怎么用，看 README；具体规则，看约定。这里只讲价值。

## 一、四个角色

和 AI 协作这件事里，有四个角色：

- **任务**——要解决的对象。人是带着任务来的，不是来聊天的；
- **人**——做判断、做决定、担责任；
- **AI**——查代码、写代码、生成内容；
- **脚本**——也就是这个工具。它不做判断、不写代码，只做两件事：
  把人-AI 之间的重复动作自动化；把协作流程固化成文件和步骤。

## 二、问题分两类

**协作中的问题**（工具能管的）：

- **上下文会丢**——会话一断，之前聊清楚的东西就没了，项目没人接得住；
- **方向会错，还发现得晚**——前面做的都白费；
- **AI 会悄悄出错**——它会把猜测当成事实写给你，你不追问，它不会主动说；
- **规矩靠自觉**——人累了、换人了、赶时间了，规矩就没人守；
- **容易半途而废**——做了一半进行不下去，最后整个放弃，是最坏的结果；
- **重复动作耗人**——复制、粘贴、建文件、开工具，每一轮都要做一遍。

**任务本身的问题**（工具不管，人和 AI 解决）：

任务复杂、需要领域知识、技术上有难点——这些靠人判断、AI 执行来解决，
工具不碰内容本身。

分清这两类很重要：这个工具的价值全部在第一类。它从不声称替你做任务。

## 三、脚本：把重复动作归零

每一轮协作都有固定动作：拼提示词、复制、粘贴发送、建回复文件、
打开编辑器、打开生成的文档、完成后提醒——脚本全部自动做掉。

另外，每一轮花了多少时间（人的、AI 的）、写了多少字、是哪个 AI 写的，
脚本自动记录，不用人操心。

## 四、流程：规范 + 留痕

流程是脚本固化下来的协作规则，作用有两个。

**规范——管这一轮别出错：**

- **先对齐再动手**：改正式代码之前必须先对齐；沟通期给 AI 的提示词
  自动带上"不要改正式代码"；
- **每步人确认**：整个流程由人按键驱动，AI 生成内容后不会自动进入
  下一步，必须等人看过、回复；
- **假设必须问出来**：AI 回复里的待确认问题会被自动提取、生成回复
  模板；人不答，事不停；
- **大任务拆小任务**：复杂任务拆成若干步骤，各自走完整流程、各自有
  明确产出。项目任何时刻停下，都是"已经完成了若干步"，而不是
  "做了一半烂尾"。（这种拆分目前一半是工具支持、一半靠流程约定。）
- **分歧看事实**：发生分歧时看谁更有理没用，看任务相关的事实——
  代码、数据、文档，查得到什么就是什么。

**留痕——管所有轮加起来不白费：**

- 每轮沟通都落成文件，存在你自己的目录里，不在任何厂商的对话框里；
- 换工具、换模型、换 AI，文件还在，新接手的按固定的重建流程读一遍
  就能接着干；
- 每份文件自动标记是哪个 AI 写的，多人、多工具接力时方便追溯；
- 每轮耗时、字数自动入账，按任务汇总成统计——协作有账可查，
  返工和重建也有记录，能成为下次改进的依据。

**规范会不会限制 AI 的发挥？** 不会。每条规范针对一个具体的坑，
不限制本事："假设必须问出来"防的是悄悄猜错，不防调研；"对齐前不碰
正式代码"防的是没确认的东西混进项目，不防写代码。边界清楚之后，
AI 反而敢多做事：什么确认了、什么没确认，不用猜；反正每轮有人把关，
精力全花在事情本身上。这套工具的限制是清楚、稳定的那种：写在固定的
提示词文件里，很短，指向具体动作。

## 五、分工：扬长避短

- **人**：说清楚要什么、做判断、检查结果。出错了是人担责任，
  所以裁决归人；
- **AI**：查代码、写代码、出方案。它做得快、做得全，所以执行归它；
- **脚本**：重复动作。

三者的精力都花在任务上，不在协作行为上损耗。

## 六、稳、准、快，顺序不能反

先把"过程不丢、协作不中断"做稳，再把"双方理解一致"做准。
快是前两样做好之后自然来的结果，不能跳过它们直接追快。
想快，正确的做法是减少返工、减少重复劳动，速度自己会出来。

## 七、它的边界

- 它能保证协作过程不散架，但**不能保证 AI 的答案一定对**。流程能保证
  假设都被问出来了、矛盾都被摆出来了；回答得对不对、说得诚不诚实，
  还是要靠人判断。
- 它适合"要求和验收标准能写清楚"的任务。审美、品味这类没法写成标准
  的任务不是不能用，但用法不一样——流程记录的是"历次商量出了什么
  结果"，而不是"谁对谁错"。
- 它的前提是**人愿意每轮都看、都确认**。没人把关，流程只是空转，
  比不用还糟——它会制造"在正经协作"的假象。

## 八、一句话

这个工具真正做出来的东西，不是某个功能，而是"协作"本身：
脚本把重复动作归零，流程把协作规范起来、把过程留下来，
人和 AI 的精力全部留给任务。

## 九、它自己就是例子

这个工具本身就是用这套流程开发出来的。它的每一项功能，都对应一次
真实使用中遇到的问题。不是先设计好再用的，是边用边长出来的。
