# 步骤1：项目骨架与配置

## 目标

建立第一版 AutoHotkey v2 工程骨架，并把高频可修改内容集中到配置目录。

## 本步产出

1. `app/` 工程目录
2. `app/config/` 配置目录
3. `app/templates/` 提示词模板目录
4. `app/AIProcess.ahk` 主程序入口
5. `settings.ini` 等集中配置文件

## 验收标准

1. 项目结构清晰
2. 配置修改位置集中
3. 后续功能都能在此结构上继续开发
