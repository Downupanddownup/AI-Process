就只有这一个主题我要提交一下，那就按强制主题提交。

关于仓库持久化文件名称，用“保存”吧。起码放两次的，一个是仓库名称，第二个就是仓库加入进来的时间，就这两个资料存一下。

添加仓库的校验规则：
1. 方案 B 只有在设置目录下才有可能会出来，毕竟我不能直接把什么都给拒绝掉。
2. 但是如果是在正规的添加仓库的过程中，方案 B 是绝对不能出来的，得直接拒绝掉。

三级弹窗列表中显示的就是一个仓库名称列表，显示全路径的。怎么可能显示不出来？这是个弹窗，又不是一个下拉框，怎么可能显示不出来？

你节点确定，你不要直接误点击了，误点击就关掉了很难。

或者说，如果设置目录它遇到了一个非标准仓库，这个仓库你就不要放进去，也不要存到里面去了，不要放进去，不要进。

经验窗口中对于已归档的目录可以做的标签，可以的。

我想清楚了，对于 set 目录，如果遇到非标准的仓库，就不要加进去。