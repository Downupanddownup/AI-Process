1. 下拉框显示文案，是偏“短标签”还是“完整标签”
答：我给了当前界面的截图，你看看应该是短还是长

2. AI判断执行 的文案是否要强调“只在 1/2 之间判断”
答：你这个问题问得莫名其妙，这个功能就是当前已经存在的功能，你直接把这个功能放进去就行了

3. 第 4 种策略的输出目录命名规则，是否需要现在就写死
答：这个策略输出的目录结构和第二步是一模一样的，只是一个是目录，一个是文件而已

4. 默认值是否只影响 UI 初始显示，还是也要写回配置
答：这种直觉策略没有回写本地文件的必要性，因为都是根据实际情况来判断

